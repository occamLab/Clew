# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!

def all_pods
  # Pods for Clew
  pod 'VectorMath', '~> 0.3'
end

target 'Clew' do
    all_pods
end

target 'Clew Dev' do
    all_pods
end
